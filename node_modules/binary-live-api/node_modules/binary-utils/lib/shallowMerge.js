"use strict";

Object.defineProperty(exports, "__esModule", {
    value: true
});

exports.default = function (arr1, arr2) {
    if (arr1.length === 0) {
        return arr2;
    }

    if (arr2.length === 0) {
        return arr1;
    }

    var maxLength = Math.max(arr1.length, arr2.length);
    var merged = [];

    var _loop = function _loop(i) {
        var arr1Element = arr1[i];
        if (arr1Element && !merged.some(function (x) {
            return x === arr1Element;
        })) {
            merged.push(arr1[i]);
        }

        var arr2Element = arr2[i];
        if (arr2Element && !merged.some(function (x) {
            return x === arr2Element;
        })) {
            merged.push(arr2[i]);
        }
    };

    for (var i = 0; i < maxLength; i++) {
        _loop(i);
    }
    return merged;
};