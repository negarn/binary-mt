'use strict';

Object.defineProperty(exports, "__esModule", {
    value: true
});

exports.default = function (str) {
    var toNumber = +str;
    if (toNumber === 0) {
        return 1..toString();
    }
    if (!!toNumber) {
        return (toNumber + 1).toString();
    }
    var captureTrailingNumber = /(\D+)(\d*)$/g;
    var match = captureTrailingNumber.exec(str);
    var incremented = match[2] ? '' + match[1] + (+match[2] + 1) : match[1] + '0';
    return incremented;
};